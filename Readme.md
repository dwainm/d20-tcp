== A simple TCP server that rolls the d20 dice when you send it any bytes of a TCP connection. ==

It will run on all IP addresse on port 8889.

You can connect to it on mac with "nc localhost 8889".
